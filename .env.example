# Where contact-form enquiries are delivered. Point this at your inbox
# automation, CRM endpoint or form relay; it receives a JSON POST with
# name, email, phone, eventType, eventDate, message and submittedAt.
# Without it, /api/contact returns 503 instead of silently dropping enquiries.
CONTACT_WEBHOOK_URL=
